export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-start justify-center gap-6 px-6">
      <p className="text-sm tracking-[0.2em] text-[color:var(--primary)]">
        PROSE NAVIGATOR
      </p>
      <h1 className="text-4xl font-bold leading-tight">
        From grievance to filed complaint — without needing to know the law
        first.
      </h1>
      <p className="text-lg text-white/70">
        Nine guided phases. Every citation verified against a primary source.
        Every deadline tracked with a safety margin. Honest about your case —
        including when the honest answer is not to file.
      </p>
      <p className="text-sm text-white/40">
        Under construction — Layer 1 (data model) and Layer 2 (verification
        service) are being built first. This is not legal advice. ProSe
        Navigator is not a law firm.
      </p>
    </main>
  );
}
